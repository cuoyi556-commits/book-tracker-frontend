# 📚 读书记录生成器 - 部署教程

## 项目结构

```
book-tracker/
├── book-tracker-backend/    # 后端API（部署到Railway.app）
│   ├── app.py
│   ├── requirements.txt
│   ├── Procfile
│   └── README.md
│
└── book-tracker-frontend/   # 前端页面（部署到Cloudflare Pages）
    └── index.html
```

---

## 🚀 部署步骤

### 第一步：部署后端到Railway.app

#### 1. 注册Railway账号

1. 访问 https://railway.app/
2. 点击右上角 **"Login"**
3. 使用 **GitHub账号登录**（推荐）
4. Railway会给你**$5免费额度/月**

#### 2. 创建新项目

**方法A：通过GitHub部署（最简单）**

1. 将 `book-tracker-backend` 文件夹上传到你的GitHub仓库
2. 在Railway点击 **"New Project"**
3. 选择 **"Deploy from GitHub repo"**
4. 选择你的仓库
5. Railway会自动检测并部署！

**方法B：通过CLI部署**

1. 安装Railway CLI：
```bash
npm install -g @railway/cli
```

2. 登录Railway：
```bash
railway login
```

3. 在 `book-tracker-backend` 文件夹运行：
```bash
cd book-tracker-backend
railway init
railway up
```

#### 3. 等待部署完成

- Railway会自动安装Python和依赖
- 首次部署需要**3-5分钟**
- 部署成功后会显示一个URL，例如：
```
https://your-api-name.up.railway.app
```

#### 4. 测试API

访问以下URL测试：
```
https://your-api-name.up.railway.app/health
```

应该返回：
```json
{"status": "healthy", "driver": "connected"}
```

---

### 第二步：部署前端到Cloudflare Pages

#### 1. 注册Cloudflare Pages

1. 访问 https://pages.cloudflare.com/
2. 使用Cloudflare账号登录（免费注册）
3. 点击 **"Create a project"**

#### 2. 上传前端文件

**方法A：直接上传（最简单）**

1. 选择 **"Upload assets"**
2. 将 `book-tracker-frontend` 文件夹中的 `index.html` 拖入
3. 点击 **"Deploy site"**

**方法B：通过GitHub部署**

1. 将 `book-tracker-frontend` 文件夹上传到GitHub
2. 在Cloudflare Pages选择 **"Connect to Git"**
3. 选择你的仓库
4. 点击 **"Begin setup"**

#### 3. 配置API地址

部署完成后：

1. 访问你的Cloudflare Pages URL
2. 在页面底部的 **"API设置"** 中
3. 填入你的Railway API地址：
```
https://your-api-name.up.railway.app
```

#### 4. 测试功能

1. 输入书名或ISBN（例如：`三体` 或 `9787536692930`）
2. 按回车搜索
3. 应该能看到书籍封面出现

---

## ✅ 完成部署

现在你可以：

1. **访问你的网站**：`https://your-project.pages.dev`
2. **输入标题和副标题**
3. **搜索并添加书籍**（支持书名或ISBN）
4. **生成3:4比例的图片**

---

## 💰 费用说明

| 服务 | 费用 | 说明 |
|------|------|------|
| Railway.app | 免费 | 每月$5额度 |
| Cloudflare Pages | 免费 | 完全免费 |
| **总计** | **$0** | 完全免费！ |

---

## 🔧 常见问题

### 1. Railway部署失败？

**检查清单：**
- [ ] `requirements.txt` 文件存在
- [ ] `Procfile` 文件存在
- [ ] 端口使用环境变量 `PORT`

### 2. 图片无法显示？

**可能原因：**
- API地址未正确配置
- 豆瓣图片失效
- Railway服务休眠（首次请求会慢）

**解决方法：**
- 检查API地址是否正确
- 尝试其他ISBN
- 等待Railway唤醒（约10秒）

### 3. Cloudflare Pages显示404？

**确保：**
- `index.html` 在根目录
- 文件名是小写的 `index.html`

### 4. 如何更新代码？

**Railway：**
- 推送代码到GitHub
- Railway会自动重新部署

**Cloudflare Pages：**
- 重新上传 `index.html`
- 或推送代码到GitHub触发自动部署

---

## 📱 分享你的网站

部署完成后，你可以：

1. **自定义域名**：在Cloudflare Pages中绑定你的域名
2. **分享链接**：直接分享Cloudflare Pages的URL

---

## 🎉 完成！

恭喜！你现在有一个完全免费的读书记录生成器了！

**需要帮助？**
- 查看Railway日志：Railway控制台 → 你的项目 → View Logs
- 查看Cloudflare日志：Cloudflare Pages → 你的项目 → Analytics

**祝你使用愉快！** 📚✨
